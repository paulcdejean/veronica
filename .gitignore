.DS_Store

# OpenTofu working files and local plans. Keep .terraform.lock.hcl committed.
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.wrangler/
.terraform/

# archive_file staging for the function source zip.
dist/

# Locally built session-driver binary (go build writes it into the cwd).
01_session_image/src/session
01_session_image/src/cmd/session/session
